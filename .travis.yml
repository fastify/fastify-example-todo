language: node_js

node_js:
  - "18"
  - "16"

services:
  - mongodb

before_script:
  - mongo todo-test --eval 'db.createUser({user:"dummy",pwd:"dummy",roles:["readWrite"]});'

notifications:
  email:
    on_success: never
    on_failure: always
